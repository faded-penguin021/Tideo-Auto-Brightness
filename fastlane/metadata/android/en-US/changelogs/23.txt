Reading the Wi-Fi network name without Location works again on Android 12 and 12L, where it had been failing silently.

Stay awake while charging now says when Android is set to a charger set this app did not write, rather than claiming dock charging is covered, and offers to replace it.

A color-correction mode this app does not recognize is still left alone, but the screen now offers to replace it.

The usage-access check no longer uses an API Android 16 deprecates.
