Reliability and safety release.

Privileged Display now reads the device's real state when opened or resumed. It checks Android's Night Light and always-on display capabilities before writing, and hides unsupported controls. Force SDR is hidden until it can use Android's live display-service path safely.

Emergency Reset and every panic control now vibrate the S.O.S. confirmation. At debug level 8, ignored control commands say why.
