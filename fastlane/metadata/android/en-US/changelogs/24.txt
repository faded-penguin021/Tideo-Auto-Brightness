Fixes false "Manual Override Detected" pauses. The app now tracks what Android actually stored, tolerates one step of rounding drift, treats a change made while adaptive brightness is on as ambiguous rather than as your input, and no longer reads a brightness change made while the screen is off as one.

Debug tools: Graph Metrics (Live Debug level 7) again times graph redraws, and a new Brightness Writes card shows requested vs stored values.
