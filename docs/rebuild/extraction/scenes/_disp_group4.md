# Disposition group 4 — graph scenes

Combined element-disposition rows for the 8 graph scenes in this extraction group.
Format: `<scene-slug>:<element> — <verb> <Target>`.
Each scene is mostly one Chart.js WebElement + Apply/Back chrome.

## alpha_graph (task557 → AAB_HTML_Graph3 → ReactivityChart alpha overlay)

- alpha_graph:elements0 — dropped(scene chrome)
- alpha_graph:elements1 — kept-as Reactivity as ReactivityChart(alpha overlay)
- alpha_graph:elements2 — dropped(scene chrome)
- alpha_graph:elements3 — merged-into Reactivity
- alpha_graph:elements4 — dropped(scene chrome)
- alpha_graph:elements5 — dropped(scene chrome)
- alpha_graph:props — dropped(scene chrome)

## brightness_graph (task663 → AAB_HTML_Graph → BrightnessCurveChart)

- brightness_graph:elements0 — dropped(scene chrome)
- brightness_graph:elements1 — kept-as Curve & Brightness as BrightnessCurveChart
- brightness_graph:elements2 — dropped(scene chrome)
- brightness_graph:elements3 — merged-into Curve & Brightness
- brightness_graph:elements4 — dropped(scene chrome)
- brightness_graph:elements5 — dropped(scene chrome)
- brightness_graph:elements6 — dropped(scene chrome)
- brightness_graph:elements7 — merged-into Curve & Brightness
- brightness_graph:props — dropped(scene chrome)

## circadian_dimming_graph (task705 → AAB_HTML_Graph7 → CircadianChart)

- circadian_dimming_graph:elements0 — dropped(scene chrome)
- circadian_dimming_graph:elements1 — kept-as Animation & Dimming as CircadianChart
- circadian_dimming_graph:elements2 — dropped(scene chrome)
- circadian_dimming_graph:elements3 — merged-into Animation & Dimming
- circadian_dimming_graph:elements4 — dropped(scene chrome)
- circadian_dimming_graph:elements5 — dropped(scene chrome)
- circadian_dimming_graph:props — dropped(scene chrome)

## dimming_graph (task556 → AAB_HTML_Graph6 → DimmingChart)

- dimming_graph:elements0 — dropped(scene chrome)
- dimming_graph:elements1 — kept-as Animation & Dimming as DimmingChart
- dimming_graph:elements2 — dropped(scene chrome)
- dimming_graph:elements3 — merged-into Animation & Dimming
- dimming_graph:elements4 — dropped(scene chrome)
- dimming_graph:elements5 — dropped(scene chrome)
- dimming_graph:props — dropped(scene chrome)

## experiment_graph (task549 → AAB_HTML_Graph4 → ExperimentChart)

- experiment_graph:elements0 — dropped(scene chrome)
- experiment_graph:elements1 — kept-as Dynamic Scale as ExperimentChart
- experiment_graph:elements2 — dropped(scene chrome)
- experiment_graph:elements3 — merged-into Dynamic Scale
- experiment_graph:elements4 — dropped(scene chrome)
- experiment_graph:elements5 — dropped(scene chrome)
- experiment_graph:props — dropped(scene chrome)

## power_draw_graph (task524 → AAB_HTML_Graph8 → PowerDrawChart)

- power_draw_graph:elements0 — dropped(scene chrome)
- power_draw_graph:elements1 — kept-as Tools as PowerDrawChart
- power_draw_graph:elements2 — dropped(scene chrome)
- power_draw_graph:elements3 — dropped(scene chrome)
- power_draw_graph:props — dropped(scene chrome)

## reactivity_graph (task703 → AAB_HTML_Graph2 → ReactivityChart)

- reactivity_graph:elements0 — dropped(scene chrome)
- reactivity_graph:elements1 — kept-as Reactivity as ReactivityChart
- reactivity_graph:elements2 — dropped(scene chrome)
- reactivity_graph:elements3 — merged-into Reactivity
- reactivity_graph:elements4 — dropped(scene chrome)
- reactivity_graph:elements5 — dropped(scene chrome)
- reactivity_graph:props — dropped(scene chrome)

## taper_graph (task657 → AAB_HTML_Graph5 → TaperChart)

- taper_graph:elements0 — dropped(scene chrome)
- taper_graph:elements1 — kept-as Dynamic Scale as TaperChart
- taper_graph:elements2 — dropped(scene chrome)
- taper_graph:elements3 — merged-into Dynamic Scale
- taper_graph:elements4 — dropped(scene chrome)
- taper_graph:elements5 — dropped(scene chrome)
- taper_graph:props — dropped(scene chrome)
