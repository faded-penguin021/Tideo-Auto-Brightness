# Disposition group 3 — scenes: profile, debug, color_filter, about, chartjs_license, user_guide

Combined element-disposition rows for the six scenes in this extraction group.
Format: `<scene-slug>:<element> — <verb> <Target>`.

## profile

- profile:elements0 — kept-as Profiles & Import/Export
- profile:background — dropped(decorative scene background; M3 provides surface)
- profile:props — dropped(handled by Compose navigation)

## debug

- debug:elements0 — kept-as Tools
- debug:elements1 — dropped(M3 nav back replaces in-scene close button)
- debug:background — dropped(decorative scene background)
- debug:props — dropped(handled by Compose navigation)

## color_filter

- color_filter:elements0 — kept-as Animation & Dimming
- color_filter:props — dropped(replaced by overlay window config in Compose/service)

## about

- about:elements0 — kept-as About+Guide+Onboarding
- about:elements1 — dropped(M3 nav back replaces in-scene close button)
- about:props — dropped(handled by Compose navigation)

## chartjs_license

- chartjs_license:elements0 — dropped(Chart.js removed)
- chartjs_license:elements1 — dropped(Chart.js removed)
- chartjs_license:props — dropped(Chart.js removed)

## user_guide

- user_guide:elements0 — kept-as About+Guide+Onboarding
- user_guide:elements1 — dropped(M3 nav back replaces in-scene close button)
- user_guide:props — dropped(handled by Compose navigation)
