<?xml version="1.0" encoding="utf-8"?>
<!--
  Home-screen widget for the Tideo Auto Brightness dashboard (owner: "the dashboard is very suited as
  a home screen widget — Brightness, lux, profile, context; service toggle/reset"). RemoteViews only
  (no Compose). Updated event-driven from the running service's publish path (battery-efficient — no
  polling, updatePeriodMillis=0). The whole surface opens the app; the two buttons toggle the service
  and re-apply (snap to auto).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <!-- Title + live status pill -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/widget_title"
            android:textColor="#ECECEC"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_status_off"
            android:textColor="#FFC107"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Applied brightness (the headline figure) -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/widget_caption_brightness"
        android:textColor="#9E9E9E"
        android:textSize="10sp" />

    <TextView
        android:id="@+id/widget_brightness"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_dash"
        android:textColor="#ECECEC"
        android:textSize="30sp"
        android:fontFamily="monospace"
        android:textStyle="bold" />

    <!-- Lux · Profile · Context readout strip -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_caption_lux"
                android:textColor="#9E9E9E"
                android:textSize="9sp" />
            <TextView
                android:id="@+id/widget_lux"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_dash"
                android:textColor="#FFC107"
                android:fontFamily="monospace"
                android:textSize="13sp" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_caption_profile"
                android:textColor="#9E9E9E"
                android:textSize="9sp" />
            <TextView
                android:id="@+id/widget_profile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_dash"
                android:textColor="#ECECEC"
                android:textSize="13sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_caption_context"
                android:textColor="#9E9E9E"
                android:textSize="9sp" />
            <TextView
                android:id="@+id/widget_context"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/widget_dash"
                android:textColor="#ECECEC"
                android:textSize="13sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>
    </LinearLayout>

    <!-- Service toggle + reset (snap to auto) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/widget_toggle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:background="@drawable/widget_button_background"
            android:text="@string/widget_turn_on"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/widget_reset"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:background="@drawable/widget_button_background"
            android:text="@string/widget_reset"
            android:textColor="#FFFFFF"
            android:textSize="12sp"
            android:textAllCaps="false" />
    </LinearLayout>
</LinearLayout>
